import React from 'react';
import Alert, { AlertType } from './components/Alert/alert';
import { type } from 'os';

function App() {
    return (
        <div>
        </div>
    );
}

export default App;
